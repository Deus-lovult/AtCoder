import java.util.*;

public class B_Trifecta {
    public static void main(String[] args){
        List<Integer> tiList = new ArrayList<>();

        Scanner sc = new Scanner(System.in);

        if (!sc.hasNext()) return ;
        int horseNum = sc.nextInt();

        while (sc.hasNext()){
            tiList.add(sc.nextInt());
        }


    }
}
